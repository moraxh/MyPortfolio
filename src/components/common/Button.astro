---
  const { class: className, ...rest } = Astro.props;
---

<a 
  class:list={[
    "bg-transparent text-text p-1.5 px-2 border-text border-2 rounded-lg flex items-center justify-center gap-1 hover:bg-text hover:text-bg hover:cursor-pointer z-20",
    className
  ]} {...rest}>
  <slot />
</a>